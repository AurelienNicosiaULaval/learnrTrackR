% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/db.R
\name{connect_postgres_tracking_db}
\alias{connect_postgres_tracking_db}
\title{Connect to a PostgreSQL tracking database}
\usage{
connect_postgres_tracking_db(..., initialize = FALSE)
}
\arguments{
\item{...}{Connection arguments passed to \code{\link[DBI:dbConnect]{DBI::dbConnect()}}, such as
\code{dbname}, \code{host}, \code{port}, \code{user}, and \code{password}.}

\item{initialize}{If \code{TRUE}, call \code{\link[=create_schema]{create_schema()}} before checking required
tables. Defaults to \code{FALSE}.}
}
\value{
A DBI connection.
}
\description{
Opens a PostgreSQL database through \code{\link[RPostgres:Postgres]{RPostgres::Postgres()}}, optionally
creates the tracking schema, and checks that all required tracking tables are
present.
}
\examples{
\dontrun{
con <- connect_postgres_tracking_db(
  dbname = "learnrtrackr",
  host = "localhost",
  user = "learnrtrackr",
  password = Sys.getenv("LEARNRTRACKR_POSTGRES_PASSWORD"),
  initialize = TRUE
)
DBI::dbDisconnect(con)
}
}
