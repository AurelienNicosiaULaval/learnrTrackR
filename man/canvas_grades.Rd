% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/canvas.R
\name{canvas_grades}
\alias{canvas_grades}
\title{Create a Canvas Gradebook CSV table}
\usage{
canvas_grades(
  con,
  tutorial_id,
  rule = c("last", "best", "first"),
  assignment = NULL,
  student_id_column = "SIS User ID",
  student_id_source = "student_id",
  grade_value = c("score", "percent"),
  digits = 2,
  include_unregistered = TRUE,
  group_id = NULL,
  section = c("group_id", "blank")
)
}
\arguments{
\item{con}{A DBI connection.}

\item{tutorial_id}{Tutorial identifier. Required.}

\item{rule}{Scoring rule passed to \code{\link[=gradebook]{gradebook()}}.}

\item{assignment}{Column name for the Canvas assignment. Defaults to
\code{tutorial_id}.}

\item{student_id_column}{Canvas identifier column to populate. Use
\code{"SIS User ID"}, \code{"SIS Login ID"}, or \code{"ID"}.}

\item{student_id_source}{Student metadata column used to populate
\code{student_id_column}. Use \code{"student_id"}, \code{"student_label"}, or \code{"email"}.}

\item{grade_value}{Which gradebook value to export. Use \code{"score"} for raw
points or \code{"percent"} for a 0-100 grade.}

\item{digits}{Number of decimal places used to round exported grades. Use
\code{NULL} to disable rounding.}

\item{include_unregistered}{If \code{TRUE}, include attempted questions that were
not registered with \code{\link[=register_questions]{register_questions()}}.}

\item{group_id}{Optional registered student group identifier. If supplied,
only students in that registered group are exported.}

\item{section}{Use \code{"group_id"} to copy registered groups to the Canvas
\code{Section} column, or \code{"blank"} to leave the section column empty.}
}
\value{
A tibble with one row per student.
}
\description{
Builds a Canvas-style Gradebook CSV table with the standard leading columns
\code{Student}, \code{ID}, \verb{SIS User ID}, \verb{SIS Login ID}, \code{Section}, and one
assignment grade column. By default, \code{learnrTrackR} student identifiers are
exported to \verb{SIS User ID} and raw gradebook scores are exported as points.
}
\details{
The exported identifiers must match the identifiers used by the target
Canvas course. When in doubt, export the current Canvas Gradebook first and
compare the identifier columns before importing.
}
\examples{
db_path <- tempfile(fileext = ".sqlite")
con <- init_tracking_db(db_path, overwrite = TRUE)
register_students(con, data.frame(student_id = "student_001", group_id = "A"))
register_questions(con, "module_01", c("q1", "q2"))
track_attempt(con, "student_001", "module_01", "q1", "mean(x)", score = 1, max_score = 1)
canvas_grades(con, tutorial_id = "module_01")
DBI::dbDisconnect(con)
}
