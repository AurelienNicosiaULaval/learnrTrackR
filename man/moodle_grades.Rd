% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/moodle.R
\name{moodle_grades}
\alias{moodle_grades}
\title{Create a Moodle-ready grade CSV table}
\usage{
moodle_grades(
  con,
  tutorial_id,
  rule = c("last", "best", "first"),
  grade_item = NULL,
  id_column = "useridnumber",
  grade_value = c("percent", "score"),
  digits = 2,
  include_unregistered = TRUE,
  group_id = NULL
)
}
\arguments{
\item{con}{A DBI connection.}

\item{tutorial_id}{Tutorial identifier. Required.}

\item{rule}{Scoring rule passed to \code{\link[=gradebook]{gradebook()}}.}

\item{grade_item}{Column name for the Moodle grade item. Defaults to
\code{tutorial_id}.}

\item{id_column}{Column name for the exported student identifier. Defaults to
\code{"useridnumber"}, a common Moodle mapping target for institutional student
identifiers.}

\item{grade_value}{Which gradebook value to export. Use \code{"percent"} for a
0-100 grade or \code{"score"} for the raw score.}

\item{digits}{Number of decimal places used to round exported grades. Use
\code{NULL} to disable rounding.}

\item{include_unregistered}{If \code{TRUE}, include attempted questions that were
not registered with \code{\link[=register_questions]{register_questions()}}.}

\item{group_id}{Optional registered student group identifier. If supplied,
only students in that registered group are exported.}
}
\value{
A tibble with one row per student.
}
\description{
Builds a simple wide grade table suitable for Moodle CSV import mapping: one
student identifier column and one grade item column. In Moodle, the teacher
maps the identifier column to the matching user field and the grade item
column to an existing grade item.
}
\examples{
db_path <- tempfile(fileext = ".sqlite")
con <- init_tracking_db(db_path, overwrite = TRUE)
register_questions(con, "module_01", c("q1", "q2"))
track_attempt(con, "student_001", "module_01", "q1", "mean(x)", score = 1, max_score = 1)
moodle_grades(con, tutorial_id = "module_01")
DBI::dbDisconnect(con)
}
