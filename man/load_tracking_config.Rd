% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/config.R
\name{load_tracking_config}
\alias{load_tracking_config}
\title{Load a tracking configuration into a database}
\usage{
load_tracking_config(
  con,
  path,
  overwrite_questions = FALSE,
  timestamp = Sys.time()
)
}
\arguments{
\item{con}{A DBI connection.}

\item{path}{Path to a YAML file or a directory containing configuration CSV
files.}

\item{overwrite_questions}{If \code{TRUE}, replace existing registered questions
for tutorials included in the configuration. If \code{FALSE}, upsert supplied
questions and keep other registered questions.}

\item{timestamp}{Creation timestamp for inserted rows. Defaults to
\code{Sys.time()}.}
}
\value{
A list with registered \code{courses}, \code{tutorials}, \code{students}, and
\code{questions} tibbles.
}
\description{
Reads a YAML or CSV-directory tracking configuration and registers courses,
tutorials, students, and questions in the tracking database.
}
\examples{
config_dir <- tempfile()
dir.create(config_dir)
readr::write_csv(
  data.frame(course_id = "stat101"),
  file.path(config_dir, "courses.csv")
)

db_path <- tempfile(fileext = ".sqlite")
con <- init_tracking_db(db_path, overwrite = TRUE)
load_tracking_config(con, config_dir)
DBI::dbDisconnect(con)
}
