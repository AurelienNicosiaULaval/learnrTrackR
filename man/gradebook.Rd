% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scores.R
\name{gradebook}
\alias{gradebook}
\title{Build a gradebook with expected questions}
\usage{
gradebook(
  con,
  tutorial_id,
  student_id = NULL,
  rule = c("last", "best", "first"),
  include_unregistered = TRUE
)
}
\arguments{
\item{con}{A DBI connection.}

\item{tutorial_id}{Tutorial identifier. Required.}

\item{student_id}{Optional student identifier. If supplied, the gradebook
includes that student even when they have no recorded attempts.}

\item{rule}{Scoring rule. \code{"last"} keeps the last attempt per question,
\code{"best"} keeps the attempt with the highest score, and \code{"first"} keeps the
first attempt.}

\item{include_unregistered}{If \code{TRUE}, attempted questions that are not in
the registered question list are included in the gradebook. If \code{FALSE},
they are ignored when registered questions exist.}
}
\value{
A tibble with \code{student_id}, \code{tutorial_id}, \code{score}, \code{max_score},
\code{percent}, \code{n_questions}, \code{n_answered}, \code{n_unanswered}, and \code{completed}.
}
\description{
Computes one gradebook row per student and tutorial. Unlike
\code{\link[=compute_scores]{compute_scores()}}, this function uses registered questions from
\code{\link[=register_questions]{register_questions()}} when available, so unanswered questions are counted in
the denominator.
}
\examples{
db_path <- tempfile(fileext = ".sqlite")
con <- init_tracking_db(db_path, overwrite = TRUE)
register_questions(con, "module_01", c("q1", "q2"))
track_attempt(con, "student_001", "module_01", "q1", "mean(x)", score = 1, max_score = 1)
gradebook(con, tutorial_id = "module_01")
DBI::dbDisconnect(con)
}
