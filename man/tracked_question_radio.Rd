% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/learnr.R
\name{tracked_question_radio}
\alias{tracked_question_radio}
\title{Create a tracked learnr radio question}
\usage{
tracked_question_radio(
  text,
  ...,
  question_id,
  tutorial_id,
  student_id,
  db_path,
  max_score = 1,
  context = NULL
)
}
\arguments{
\item{text}{Question text passed to \code{\link[learnr:question_radio]{learnr::question_radio()}}.}

\item{...}{Answers created with \code{\link[learnr:answer]{learnr::answer()}}, followed by optional
arguments passed to \code{\link[learnr:question_radio]{learnr::question_radio()}}.}

\item{question_id}{Question identifier stored in the tracking database.}

\item{tutorial_id}{Tutorial identifier stored in the tracking database.}

\item{student_id}{Student identifier stored in the tracking database.}

\item{db_path}{Path to the SQLite tracking database.}

\item{max_score}{Maximum score stored for the question. Defaults to \code{1}.}

\item{context}{Optional context returned by \code{\link[=setup_learnr_tracking]{setup_learnr_tracking()}}. If
supplied, \code{student_id}, \code{tutorial_id}, and \code{db_path} are read from the
context unless explicitly provided.}
}
\value{
A \code{learnr} radio question object with tracking metadata.
}
\description{
Wraps \code{\link[learnr:question_radio]{learnr::question_radio()}} and records each submitted answer in a
\code{learnrTrackR} SQLite database when \code{learnr} evaluates the question. This
function is intended for small prototypes and local teaching workflows.
}
\details{
The tracking implementation uses the public \code{learnr::question_is_correct()}
S3 extension point. It does not use browser JavaScript interception.
}
\examples{
if (requireNamespace("learnr", quietly = TRUE)) {
  db_path <- tempfile(fileext = ".sqlite")
  question <- tracked_question_radio(
    "What is 2 + 2?",
    learnr::answer("3"),
    learnr::answer("4", correct = TRUE),
    question_id = "q1",
    tutorial_id = "module_01",
    student_id = "student_001",
    db_path = db_path
  )
}
}
