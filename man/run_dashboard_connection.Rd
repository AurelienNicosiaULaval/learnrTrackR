% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dashboard.R
\name{run_dashboard_connection}
\alias{run_dashboard_connection}
\title{Run the teacher dashboard from an open DBI connection}
\usage{
run_dashboard_connection(
  con,
  tutorial_id = NULL,
  rule = c("last", "best", "first"),
  include_unregistered = TRUE,
  group_id = NULL,
  host = "127.0.0.1",
  access_token = NULL,
  token_envvar = "LEARNRTRACKR_DASHBOARD_TOKEN",
  allow_remote = FALSE,
  ...
)
}
\arguments{
\item{con}{A DBI connection.}

\item{tutorial_id}{Optional tutorial identifier. If \code{NULL}, the first
available tutorial found in the database is used.}

\item{rule}{Scoring rule passed to \code{\link[=gradebook]{gradebook()}}.}

\item{include_unregistered}{If \code{TRUE}, include attempted questions that were
not registered with \code{\link[=register_questions]{register_questions()}}.}

\item{group_id}{Optional registered student group identifier. If supplied,
only students registered in that group are included.}

\item{host}{Host passed to \code{\link[shiny:runApp]{shiny::runApp()}}. Defaults to \code{"127.0.0.1"} for
local-only use.}

\item{access_token}{Optional dashboard token. If supplied, the dashboard asks
for this token before showing data.}

\item{token_envvar}{Environment variable used to read a dashboard token when
\code{access_token} is \code{NULL}. Set to \code{NULL} to disable environment lookup.}

\item{allow_remote}{If \code{FALSE}, refuse to run on a non-local host unless an
access token is configured.}

\item{...}{Additional arguments passed to \code{\link[shiny:runApp]{shiny::runApp()}}.}
}
\value{
The return value of \code{\link[shiny:runApp]{shiny::runApp()}}.
}
\description{
Opens the same Shiny dashboard as \code{\link[=run_dashboard]{run_dashboard()}}, but reads data from an
existing DBI connection instead of a SQLite file path. The caller owns the
connection and is responsible for disconnecting it after the dashboard stops.
}
\examples{
\dontrun{
db_path <- "learnrtrackr.sqlite"
con <- connect_tracking_db(db_path)
run_dashboard_connection(con)
DBI::dbDisconnect(con)
}
}
