% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scores.R
\name{compute_scores}
\alias{compute_scores}
\title{Compute summarized tutorial scores}
\usage{
compute_scores(con, tutorial_id = NULL, rule = c("last", "best", "first"))
}
\arguments{
\item{con}{A DBI connection.}

\item{tutorial_id}{Optional tutorial identifier used to filter attempts.}

\item{rule}{Scoring rule. \code{"last"} keeps the last attempt per question,
\code{"best"} keeps the attempt with the highest score, and \code{"first"} keeps the
first attempt.}
}
\value{
A tibble with \code{student_id}, \code{tutorial_id}, \code{score}, \code{max_score},
\code{percent}, \code{n_questions}, and \code{n_answered}.
}
\description{
Computes one score per student and tutorial from recorded attempts. For each
question, the selected attempt is determined by \code{rule}.
}
\examples{
db_path <- tempfile(fileext = ".sqlite")
con <- init_tracking_db(db_path, overwrite = TRUE)
track_attempt(con, "student_001", "module_01", "q1", "mean(x)", score = 1, max_score = 1)
compute_scores(con, tutorial_id = "module_01")
DBI::dbDisconnect(con)
}
