% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/learnr.R
\name{setup_learnr_tracking}
\alias{setup_learnr_tracking}
\title{Set up tracking for a learnr tutorial}
\usage{
setup_learnr_tracking(
  tutorial_id,
  student_id = get_tracking_student_id(),
  db_path = Sys.getenv("LEARNRTRACKR_DB", unset = file.path(tempdir(),
    paste0(tutorial_id, ".sqlite"))),
  group_id = Sys.getenv("LEARNRTRACKR_GROUP_ID", unset = NA_character_),
  config_path = NULL,
  student_label = student_id,
  overwrite = FALSE,
  register_student = TRUE
)
}
\arguments{
\item{tutorial_id}{Tutorial identifier stored in the tracking database.}

\item{student_id}{Student identifier. Defaults to \code{\link[=get_tracking_student_id]{get_tracking_student_id()}}.}

\item{db_path}{Path to the SQLite tracking database. Defaults to the
\code{LEARNRTRACKR_DB} environment variable, or a temporary SQLite file based on
\code{tutorial_id} when the variable is missing.}

\item{group_id}{Optional learner group. Defaults to the
\code{LEARNRTRACKR_GROUP_ID} environment variable, or \code{NA_character_}.}

\item{config_path}{Optional YAML file or CSV directory loaded with
\code{\link[=load_tracking_config]{load_tracking_config()}}. Use \code{NULL} to skip configuration loading.}

\item{student_label}{Optional label for the current learner. Defaults to
\code{student_id}.}

\item{overwrite}{If \code{TRUE}, remove an existing SQLite database before
initialization.}

\item{register_student}{If \code{TRUE}, register the current learner in the
\code{students} table.}
}
\value{
A \code{learnrTrackR_context} list with \code{student_id}, \code{tutorial_id},
\code{db_path}, \code{group_id}, and \code{config_path}.
}
\description{
Initializes the tracking database, optionally loads a tracking configuration,
registers the current learner, and returns a small context object. The context
can then be passed to \code{\link[=tracked_question]{tracked_question()}}, \code{\link[=tracked_question_radio]{tracked_question_radio()}},
\code{\link[=tracked_question_checkbox]{tracked_question_checkbox()}}, \code{\link[=tracked_question_text]{tracked_question_text()}},
\code{\link[=tracked_question_numeric]{tracked_question_numeric()}}, \code{\link[=track_gradethis_attempt]{track_gradethis_attempt()}}, and
\code{\link[=open_learnr_tracking_db]{open_learnr_tracking_db()}}.
}
\details{
This helper is intended for the setup chunk of a \code{learnr} tutorial. It keeps
the tutorial code explicit while avoiding repeated \code{student_id},
\code{tutorial_id}, and \code{db_path} arguments in each tracked question.
}
\examples{
db_path <- tempfile(fileext = ".sqlite")
tracking <- setup_learnr_tracking(
  tutorial_id = "module_01",
  student_id = "student_001",
  db_path = db_path
)

con <- open_learnr_tracking_db(tracking)
DBI::dbDisconnect(con)
}
