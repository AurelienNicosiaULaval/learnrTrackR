% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/privacy.R
\name{pseudonymise_results}
\alias{pseudonymise_results}
\title{Pseudonymise student identifiers in result tables}
\usage{
pseudonymise_results(
  x,
  id_column = "student_id",
  prefix = "student",
  drop_columns = c("student_label", "email"),
  key = NULL
)
}
\arguments{
\item{x}{A data frame or a list of data frames, such as the output of
\code{\link[=tracking_export_data]{tracking_export_data()}}.}

\item{id_column}{Name of the student identifier column. Defaults to
\code{"student_id"}.}

\item{prefix}{Prefix used to create pseudonyms. Defaults to \code{"student"}.}

\item{drop_columns}{Columns removed from the returned data after
pseudonymisation. Defaults to \code{student_label} and \code{email}.}

\item{key}{Optional existing key with columns \code{id_column} and \code{pseudonym}.
Use this to keep the same mapping across separate exports.}
}
\value{
A list with \code{data}, the pseudonymised data in the same shape as \code{x},
and \code{key}, a tibble mapping original identifiers to pseudonyms.
}
\description{
Replaces student identifiers with stable sequential pseudonyms and returns a
separate key. The key remains identifying and should be stored separately
from exported results.
}
\examples{
results <- data.frame(
  student_id = c("student_001", "student_002"),
  email = c("a@example.org", "b@example.org"),
  score = c(8, 9)
)
pseudonymise_results(results)
}
