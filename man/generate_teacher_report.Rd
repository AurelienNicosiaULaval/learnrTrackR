% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/report.R
\name{generate_teacher_report}
\alias{generate_teacher_report}
\title{Generate an HTML teacher report}
\usage{
generate_teacher_report(
  con,
  path,
  tutorial_id,
  rule = c("last", "best", "first"),
  include_unregistered = TRUE,
  group_id = NULL,
  max_mean_percent = 60,
  max_student_percent = 60,
  min_students = 1,
  min_attempts = 1,
  quiet = TRUE
)
}
\arguments{
\item{con}{A DBI connection.}

\item{path}{Output HTML file path.}

\item{tutorial_id}{Tutorial identifier. Required.}

\item{rule}{Scoring rule passed to \code{\link[=summarise_questions]{summarise_questions()}} and
\code{\link[=summarise_students]{summarise_students()}}.}

\item{include_unregistered}{If \code{TRUE}, include attempted questions not
registered with \code{\link[=register_questions]{register_questions()}}.}

\item{group_id}{Optional registered student group identifier.}

\item{max_mean_percent}{Maximum mean question percent used by
\code{\link[=detect_difficult_questions]{detect_difficult_questions()}}.}

\item{max_student_percent}{Maximum student percent used by
\code{\link[=detect_stalled_students]{detect_stalled_students()}}.}

\item{min_students}{Minimum student count for difficult-question detection.}

\item{min_attempts}{Minimum attempt count for difficult-question and
stalled-student detection.}

\item{quiet}{If \code{TRUE}, suppress rendering output from \code{\link[rmarkdown:render]{rmarkdown::render()}}.}
}
\value{
The output path, invisibly.
}
\description{
Renders a small HTML report for one tutorial using question summaries,
student summaries, difficult-question flags, and stalled-student flags.
}
\examples{
\dontrun{
db_path <- tempfile(fileext = ".sqlite")
con <- init_tracking_db(db_path, overwrite = TRUE)
register_questions(con, "module_01", c("q1", "q2"))
track_attempt(con, "student_001", "module_01", "q1", "mean(x)", score = 1, max_score = 1)
generate_teacher_report(con, tempfile(fileext = ".html"), "module_01")
DBI::dbDisconnect(con)
}
}
