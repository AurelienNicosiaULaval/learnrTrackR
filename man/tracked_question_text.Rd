% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/learnr.R
\name{tracked_question_text}
\alias{tracked_question_text}
\title{Create a tracked learnr text question}
\usage{
tracked_question_text(
  text,
  ...,
  question_id,
  tutorial_id,
  student_id,
  db_path,
  max_score = 1,
  correct = "Correct!",
  incorrect = "Incorrect",
  try_again = incorrect,
  allow_retry = FALSE,
  random_answer_order = FALSE,
  placeholder = "Enter answer here...",
  trim = TRUE,
  rows = NULL,
  cols = NULL,
  options = list()
)
}
\arguments{
\item{text}{Question text passed to \code{\link[learnr:question_text]{learnr::question_text()}}.}

\item{...}{Answers created with \code{\link[learnr:answer]{learnr::answer()}} or \code{\link[learnr:answer]{learnr::answer_fn()}},
followed by optional arguments passed to \code{\link[learnr:question_text]{learnr::question_text()}}.}

\item{question_id}{Question identifier stored in the tracking database.}

\item{tutorial_id}{Tutorial identifier stored in the tracking database.}

\item{student_id}{Student identifier stored in the tracking database.}

\item{db_path}{Path to the SQLite tracking database.}

\item{max_score}{Maximum score stored for the question. Defaults to \code{1}.}

\item{correct}{Text shown by \code{learnr} for a correct answer.}

\item{incorrect}{Text shown by \code{learnr} for an incorrect answer.}

\item{try_again}{Text shown by \code{learnr} for an incorrect retry.}

\item{allow_retry}{Whether \code{learnr} should allow retries.}

\item{random_answer_order}{Passed to \code{\link[learnr:question_text]{learnr::question_text()}}.}

\item{placeholder}{Placeholder text for the input.}

\item{trim}{Whether \code{learnr} should trim whitespace before checking.}

\item{rows, cols}{Optional text area dimensions.}

\item{options}{Additional \code{learnr} question options.}
}
\value{
A \code{learnr} text question object with tracking metadata.
}
\description{
Wraps \code{\link[learnr:question_text]{learnr::question_text()}} and records each submitted answer in a
\code{learnrTrackR} SQLite database when \code{learnr} evaluates the question.
}
\details{
The tracking implementation uses the public \code{learnr::question_is_correct()}
S3 extension point. It does not use browser JavaScript interception.
}
\examples{
if (requireNamespace("learnr", quietly = TRUE)) {
  db_path <- tempfile(fileext = ".sqlite")
  question <- tracked_question_text(
    "Type the word mean.",
    learnr::answer("mean", correct = TRUE),
    question_id = "q1",
    tutorial_id = "module_01",
    student_id = "student_001",
    db_path = db_path
  )
}
}
