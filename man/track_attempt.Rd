% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/track.R
\name{track_attempt}
\alias{track_attempt}
\title{Record a simulated tutorial attempt}
\usage{
track_attempt(
  con,
  student_id,
  tutorial_id,
  question_id,
  submitted_answer,
  grade_status = NA_character_,
  score = NA_real_,
  max_score = NA_real_,
  feedback = NA_character_,
  session_id = NULL,
  attempt_number = NULL,
  timestamp = Sys.time()
)
}
\arguments{
\item{con}{A DBI connection.}

\item{student_id}{Student identifier.}

\item{tutorial_id}{Tutorial identifier.}

\item{question_id}{Question identifier.}

\item{submitted_answer}{Submitted answer as text.}

\item{grade_status}{Optional grading status, such as \code{"correct"} or
\code{"partial"}.}

\item{score}{Optional numeric score.}

\item{max_score}{Optional numeric maximum score.}

\item{feedback}{Optional feedback text.}

\item{session_id}{Optional session identifier.}

\item{attempt_number}{Optional positive integer attempt number.}

\item{timestamp}{Attempt timestamp. Defaults to \code{Sys.time()}.}
}
\value{
The inserted \code{attempt_id}, invisibly.
}
\description{
Inserts one attempt in the \code{attempts} table. If \code{session_id} is \code{NULL}, a
simple session identifier is generated from the student, tutorial, and date.
If \code{attempt_number} is \code{NULL}, the next number is computed for the
student-tutorial-question combination.
}
\examples{
db_path <- tempfile(fileext = ".sqlite")
con <- init_tracking_db(db_path, overwrite = TRUE)

track_attempt(
  con = con,
  student_id = "student_001",
  tutorial_id = "module_01",
  question_id = "q1",
  submitted_answer = "mean(x)",
  grade_status = "correct",
  score = 1,
  max_score = 1,
  feedback = "Correct."
)

DBI::dbDisconnect(con)
}
