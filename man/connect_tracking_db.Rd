% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/db.R
\name{connect_tracking_db}
\alias{connect_tracking_db}
\title{Connect to an existing tracking SQLite database}
\usage{
connect_tracking_db(path)
}
\arguments{
\item{path}{Path to an existing SQLite database file.}
}
\value{
A DBI connection.
}
\description{
Opens a SQLite database and checks that all required tracking tables are
present. Use \code{\link[=init_tracking_db]{init_tracking_db()}} to create a new database or to add missing
tables to an existing file.
}
\examples{
db_path <- tempfile(fileext = ".sqlite")
con <- init_tracking_db(db_path, overwrite = TRUE)
DBI::dbDisconnect(con)

con <- connect_tracking_db(db_path)
DBI::dbDisconnect(con)
}
