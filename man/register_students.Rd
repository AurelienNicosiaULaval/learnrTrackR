% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/students.R
\name{register_students}
\alias{register_students}
\title{Register expected students}
\usage{
register_students(con, students, timestamp = Sys.time())
}
\arguments{
\item{con}{A DBI connection.}

\item{students}{A data frame with a required \code{student_id} column and optional
\code{student_label}, \code{email}, and \code{group_id} columns. A character vector is
treated as a vector of student identifiers.}

\item{timestamp}{Creation timestamp for inserted rows. Defaults to
\code{Sys.time()}.}
}
\value{
A tibble of registered students.
}
\description{
Stores expected student identifiers in the \code{students} table. Registered
students can be used by \code{\link[=track_attempt]{track_attempt()}} to reject attempts from unknown
identifiers when \code{require_registered_student = TRUE}.
}
\examples{
db_path <- tempfile(fileext = ".sqlite")
con <- init_tracking_db(db_path, overwrite = TRUE)
register_students(
  con,
  data.frame(
    student_id = c("student_001", "student_002"),
    group_id = c("A", "A")
  )
)
DBI::dbDisconnect(con)
}
