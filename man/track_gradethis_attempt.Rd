% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/learnr.R
\name{track_gradethis_attempt}
\alias{track_gradethis_attempt}
\title{Track an attempt from a gradethis check}
\usage{
track_gradethis_attempt(
  con,
  student_id,
  tutorial_id,
  question_id,
  submitted_answer,
  correct,
  feedback = NULL,
  score = NULL,
  max_score = 1,
  grade_status = NULL,
  session_id = NULL,
  attempt_number = NULL,
  timestamp = Sys.time(),
  context = NULL
)
}
\arguments{
\item{con}{A DBI connection.}

\item{student_id}{Student identifier.}

\item{tutorial_id}{Tutorial identifier.}

\item{question_id}{Question identifier.}

\item{submitted_answer}{Submitted answer or code. In a \code{grade_this()} check,
this is usually \code{.user_code}.}

\item{correct}{Logical scalar indicating whether the submission is correct.}

\item{feedback}{Feedback returned to the learner and stored in the database.
If \code{NULL}, a simple default message is chosen from \code{correct}.}

\item{score}{Numeric score to store. If \code{NULL}, stores \code{max_score} when
\code{correct = TRUE} and \code{0} otherwise.}

\item{max_score}{Numeric maximum score. Defaults to \code{1}.}

\item{grade_status}{Character status to store. If \code{NULL}, stores
\code{"correct"} when \code{correct = TRUE} and \code{"incorrect"} otherwise.}

\item{session_id}{Optional session identifier passed to \code{\link[=track_attempt]{track_attempt()}}.}

\item{attempt_number}{Optional attempt number passed to \code{\link[=track_attempt]{track_attempt()}}.}

\item{timestamp}{Attempt timestamp. Defaults to \code{Sys.time()}.}

\item{context}{Optional context returned by \code{\link[=setup_learnr_tracking]{setup_learnr_tracking()}}. If
supplied, \code{student_id}, \code{tutorial_id}, and the database connection can be
resolved from the context.}
}
\value{
A \code{gradethis_graded} object returned by \code{gradethis::graded()}.
}
\description{
Records one attempt and returns a \code{gradethis} graded result. This helper is
intended for explicit use inside a \code{gradethis::grade_this()} check chunk in a
\code{learnr} tutorial. It does not intercept \code{learnr} submissions automatically.
}
\examples{
if (requireNamespace("gradethis", quietly = TRUE)) {
  db_path <- tempfile(fileext = ".sqlite")
  con <- init_tracking_db(db_path, overwrite = TRUE)

  track_gradethis_attempt(
    con = con,
    student_id = "student_001",
    tutorial_id = "module_01",
    question_id = "q1",
    submitted_answer = "2 + 2",
    correct = TRUE,
    feedback = "Correct."
  )

  DBI::dbDisconnect(con)

  tracking <- setup_learnr_tracking(
    tutorial_id = "module_01",
    student_id = "student_001",
    db_path = db_path
  )

  track_gradethis_attempt(
    context = tracking,
    question_id = "q2",
    submitted_answer = "mean(x)",
    correct = FALSE
  )
}
}
