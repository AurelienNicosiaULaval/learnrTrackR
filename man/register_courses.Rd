% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/courses.R
\name{register_courses}
\alias{register_courses}
\title{Register courses}
\usage{
register_courses(con, courses, timestamp = Sys.time())
}
\arguments{
\item{con}{A DBI connection.}

\item{courses}{A data frame with a required \code{course_id} column and optional
\code{course_label} and \code{semester} columns. A character vector is treated as a
vector of course identifiers.}

\item{timestamp}{Creation timestamp for inserted rows. Defaults to
\code{Sys.time()}.}
}
\value{
A tibble of registered courses.
}
\description{
Stores course metadata in the \code{courses} table.
}
\examples{
db_path <- tempfile(fileext = ".sqlite")
con <- init_tracking_db(db_path, overwrite = TRUE)
register_courses(con, data.frame(course_id = "stat101", semester = "W2026"))
DBI::dbDisconnect(con)
}
