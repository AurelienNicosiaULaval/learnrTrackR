% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pilot_readiness.R
\name{check_pilot_readiness}
\alias{check_pilot_readiness}
\title{Check pilot readiness before teacher exports}
\usage{
check_pilot_readiness(
  con,
  tutorial_id,
  group_id = NULL,
  rule = c("last", "best", "first"),
  include_unregistered = TRUE,
  require_attempts = FALSE,
  require_all_students_attempted = FALSE,
  stop_on_error = FALSE
)
}
\arguments{
\item{con}{A DBI connection.}

\item{tutorial_id}{Tutorial identifier. Required.}

\item{group_id}{Optional registered student group identifier. If supplied,
checks and export counts are scoped to that group.}

\item{rule}{Scoring rule passed to \code{\link[=gradebook]{gradebook()}} and export helpers.}

\item{include_unregistered}{If \code{TRUE}, gradebook calculations include
attempted questions that were not registered with \code{\link[=register_questions]{register_questions()}}.}

\item{require_attempts}{If \code{TRUE}, no recorded attempt in the selected scope
is reported as an error. If \code{FALSE}, it is reported as a warning.}

\item{require_all_students_attempted}{If \code{TRUE}, registered students without
attempts in the selected scope are reported as an error. If \code{FALSE}, they
are reported as a warning.}

\item{stop_on_error}{If \code{TRUE}, throw an error when any check has status
\code{"error"}.}
}
\value{
A tibble with one row per check and columns \code{check}, \code{status}, \code{n},
\code{message}, and \code{details}.
}
\description{
Runs a compact set of consistency checks before using tracked tutorial data
for a controlled pilot or LMS-oriented export. The checks focus on registered
students, registered questions, unexpected learners, unregistered attempted
questions, incomplete gradebook rows, and Moodle/Canvas export row
consistency.
}
\examples{
db_path <- tempfile(fileext = ".sqlite")
con <- init_tracking_db(db_path, overwrite = TRUE)
register_students(con, data.frame(student_id = "student_001", group_id = "A"))
register_questions(con, "module_01", "q1")
track_attempt(con, "student_001", "module_01", "q1", "mean(x)", score = 1, max_score = 1)
check_pilot_readiness(con, tutorial_id = "module_01", group_id = "A")
DBI::dbDisconnect(con)
}
