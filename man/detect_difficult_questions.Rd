% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analytics.R
\name{detect_difficult_questions}
\alias{detect_difficult_questions}
\title{Detect potentially difficult questions}
\usage{
detect_difficult_questions(
  con,
  tutorial_id,
  rule = c("last", "best", "first"),
  include_unregistered = TRUE,
  group_id = NULL,
  max_mean_percent = 60,
  min_students = 1,
  min_attempts = 1
)
}
\arguments{
\item{con}{A DBI connection.}

\item{tutorial_id}{Tutorial identifier. Required.}

\item{rule}{Scoring rule used to select one attempt per student and question.}

\item{include_unregistered}{If \code{TRUE}, include attempted questions not
registered with \code{\link[=register_questions]{register_questions()}}.}

\item{group_id}{Optional registered student group identifier. If supplied,
only attempts from students in that group are included.}

\item{max_mean_percent}{Maximum mean percent used to flag a question.
Defaults to \code{60}.}

\item{min_students}{Minimum number of students with a selected attempt.
Defaults to \code{1}.}

\item{min_attempts}{Minimum number of raw attempts. Defaults to \code{1}.}
}
\value{
A tibble of flagged question summaries, sorted from lowest mean
percent to highest.
}
\description{
Filters \code{\link[=summarise_questions]{summarise_questions()}} to questions whose mean percent is below a
threshold and that have enough observed activity.
}
\examples{
db_path <- tempfile(fileext = ".sqlite")
con <- init_tracking_db(db_path, overwrite = TRUE)
track_attempt(con, "student_001", "module_01", "q1", "wrong", score = 0, max_score = 1)
detect_difficult_questions(con, "module_01")
DBI::dbDisconnect(con)
}
