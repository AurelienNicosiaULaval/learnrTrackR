% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dashboard.R
\name{run_dashboard_postgres}
\alias{run_dashboard_postgres}
\title{Run the teacher dashboard from PostgreSQL}
\usage{
run_dashboard_postgres(
  dbname = Sys.getenv("LEARNRTRACKR_POSTGRES_DB", unset = "learnrtrackr"),
  postgres_host = Sys.getenv("LEARNRTRACKR_POSTGRES_HOST", unset = "127.0.0.1"),
  postgres_port = as.integer(Sys.getenv("LEARNRTRACKR_POSTGRES_PORT", unset = "5432")),
  postgres_user = Sys.getenv("LEARNRTRACKR_POSTGRES_USER", unset = "learnrtrackr"),
  postgres_password = Sys.getenv("LEARNRTRACKR_POSTGRES_PASSWORD", unset = ""),
  postgres_schema = Sys.getenv("LEARNRTRACKR_POSTGRES_SCHEMA", unset = ""),
  initialize = FALSE,
  tutorial_id = NULL,
  rule = c("last", "best", "first"),
  include_unregistered = TRUE,
  group_id = Sys.getenv("LEARNRTRACKR_PILOT_GROUP_ID", unset = ""),
  host = "127.0.0.1",
  access_token = NULL,
  token_envvar = "LEARNRTRACKR_DASHBOARD_TOKEN",
  allow_remote = FALSE,
  ...
)
}
\arguments{
\item{dbname}{PostgreSQL database name. Defaults to
\code{LEARNRTRACKR_POSTGRES_DB}, or \code{"learnrtrackr"} when unset.}

\item{postgres_host}{PostgreSQL host. Defaults to
\code{LEARNRTRACKR_POSTGRES_HOST}, or \code{"127.0.0.1"} when unset.}

\item{postgres_port}{PostgreSQL port. Defaults to
\code{LEARNRTRACKR_POSTGRES_PORT}, or \code{5432} when unset.}

\item{postgres_user}{PostgreSQL user. Defaults to
\code{LEARNRTRACKR_POSTGRES_USER}, or \code{"learnrtrackr"} when unset.}

\item{postgres_password}{Optional PostgreSQL password. Defaults to
\code{LEARNRTRACKR_POSTGRES_PASSWORD}.}

\item{postgres_schema}{Optional PostgreSQL schema. Defaults to
\code{LEARNRTRACKR_POSTGRES_SCHEMA}.}

\item{initialize}{If \code{TRUE}, create \code{postgres_schema} when supplied and
create missing tracking tables before launching the dashboard.}

\item{tutorial_id}{Optional tutorial identifier selected at startup. If
\code{NULL}, the first available tutorial is selected.}

\item{rule}{Scoring rule passed to \code{\link[=gradebook]{gradebook()}}.}

\item{include_unregistered}{If \code{TRUE}, include attempted questions that were
not registered with \code{\link[=register_questions]{register_questions()}}.}

\item{group_id}{Optional registered student group identifier selected at
startup. If supplied, the dashboard is filtered to students in that group.}

\item{host}{Host passed to \code{\link[shiny:runApp]{shiny::runApp()}}. Defaults to \code{"127.0.0.1"} for
local-only use.}

\item{access_token}{Optional dashboard token. If supplied, the dashboard asks
for this token before showing data.}

\item{token_envvar}{Environment variable used to read a dashboard token when
\code{access_token} is \code{NULL}. Set to \code{NULL} to disable environment lookup.}

\item{allow_remote}{If \code{FALSE}, refuse to run on a non-local host unless an
access token is configured.}

\item{...}{Additional arguments passed to \code{\link[shiny:runApp]{shiny::runApp()}}.}
}
\value{
The return value of \code{\link[shiny:runApp]{shiny::runApp()}}.
}
\description{
Connects to a PostgreSQL tracking database and opens the teacher dashboard.
Connection settings default to \verb{LEARNRTRACKR_POSTGRES_*} environment
variables. If \code{postgres_schema} is supplied, the PostgreSQL \code{search_path} is
set to that schema before dashboard data are read.
}
\examples{
\dontrun{
run_dashboard_postgres(
  postgres_schema = "learnrtrackr_pilot",
  group_id = "A",
  access_token = Sys.getenv("LEARNRTRACKR_DASHBOARD_TOKEN")
)
}
}
