% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/questions.R
\name{register_questions}
\alias{register_questions}
\title{Register expected tutorial questions}
\usage{
register_questions(
  con,
  tutorial_id,
  questions,
  default_max_score = 1,
  overwrite = FALSE,
  timestamp = Sys.time()
)
}
\arguments{
\item{con}{A DBI connection.}

\item{tutorial_id}{Tutorial identifier.}

\item{questions}{A data frame with a required \code{question_id} column and
optional \code{question_label}, \code{question_type}, and \code{max_score} columns. A
character vector is treated as a vector of question identifiers.}

\item{default_max_score}{Score used when \code{max_score} is missing. Defaults to
\code{1}.}

\item{overwrite}{If \code{TRUE}, delete existing registered questions for
\code{tutorial_id} before inserting \code{questions}. If \code{FALSE}, upsert the supplied
questions and leave other registered questions unchanged.}

\item{timestamp}{Creation timestamp for inserted rows. Defaults to
\code{Sys.time()}.}
}
\value{
A tibble of registered questions.
}
\description{
Stores the expected questions for a tutorial in the \code{questions} table. These
registered questions are used by \code{\link[=gradebook]{gradebook()}} to count unanswered questions
in the score denominator.
}
\examples{
db_path <- tempfile(fileext = ".sqlite")
con <- init_tracking_db(db_path, overwrite = TRUE)
register_questions(
  con,
  tutorial_id = "module_01",
  questions = data.frame(
    question_id = c("q1", "q2"),
    max_score = c(1, 2)
  )
)
DBI::dbDisconnect(con)
}
