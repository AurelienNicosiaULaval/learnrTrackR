% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/learnr.R
\name{tracked_question}
\alias{tracked_question}
\title{Create a tracked learnr question}
\usage{
tracked_question(
  text,
  ...,
  type = c("auto", "single", "multiple", "radio", "checkbox", "text", "numeric",
    "learnr_radio", "learnr_checkbox", "learnr_text", "learnr_numeric"),
  question_id,
  tutorial_id,
  student_id,
  db_path,
  max_score = 1,
  context = NULL
)
}
\arguments{
\item{text}{Question text.}

\item{...}{Answers and type-specific arguments passed to the selected
question helper.}

\item{type}{Question type. Supported values are \code{"auto"}, \code{"radio"},
\code{"checkbox"}, \code{"text"}, and \code{"numeric"}, with aliases \code{"single"},
\code{"multiple"}, \code{"learnr_radio"}, \code{"learnr_checkbox"}, \code{"learnr_text"}, and
\code{"learnr_numeric"}.}

\item{question_id}{Question identifier stored in the tracking database.}

\item{tutorial_id}{Tutorial identifier stored in the tracking database.}

\item{student_id}{Student identifier stored in the tracking database.}

\item{db_path}{Path to the SQLite tracking database.}

\item{max_score}{Maximum score stored for the question. Defaults to \code{1}.}

\item{context}{Optional context returned by \code{\link[=setup_learnr_tracking]{setup_learnr_tracking()}}. If
supplied, \code{student_id}, \code{tutorial_id}, and \code{db_path} are read from the
context unless explicitly provided.}
}
\value{
A tracked \code{learnr} question object.
}
\description{
Creates a tracked \code{learnr} question and dispatches to the appropriate
question-specific helper: \code{\link[=tracked_question_radio]{tracked_question_radio()}},
\code{\link[=tracked_question_checkbox]{tracked_question_checkbox()}}, \code{\link[=tracked_question_text]{tracked_question_text()}}, or
\code{\link[=tracked_question_numeric]{tracked_question_numeric()}}.
}
\details{
For \code{type = "auto"}, the helper follows the same simple convention as
\code{learnr::question()} for literal answer choices: one correct answer creates a
radio question, and more than one correct answer creates a checkbox question.
Text and numeric questions should be requested explicitly with \code{type = "text"}
or \code{type = "numeric"}.
}
\examples{
if (requireNamespace("learnr", quietly = TRUE)) {
  db_path <- tempfile(fileext = ".sqlite")
  question <- tracked_question(
    "What is 2 + 2?",
    learnr::answer("3"),
    learnr::answer("4", correct = TRUE),
    type = "radio",
    question_id = "q1",
    tutorial_id = "module_01",
    student_id = "student_001",
    db_path = db_path
  )

  tracking <- setup_learnr_tracking(
    tutorial_id = "module_01",
    student_id = "student_001",
    db_path = db_path
  )

  tracked_question(
    "What is 3 + 3?",
    learnr::answer("5"),
    learnr::answer("6", correct = TRUE),
    type = "radio",
    question_id = "q2",
    context = tracking
  )
}
}
